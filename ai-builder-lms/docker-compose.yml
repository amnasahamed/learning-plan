version: '3.8'

services:
  ai-builder-lms:
    build: .
    container_name: ai-builder-lms
    ports:
      - "737:3000"
    volumes:
      # Persist user progress and user data
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET:-ai-builder-lms-secret-key-2024}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
